#include <stdio.h>
#include <stdlib.h>

/*Desarrollar una función reciba un vector y un valor e inserte el valor en el vector de forma ordenada.
El valor 0 se indica el fin de los datos.
Nota: el vector siempre está ordenado.
El prototipo de la función es el siguiente:*/

void insertar(<definir> vec, <definir> valor);

int main()
{
    printf("Hello world!\n");
    return 0;
}
